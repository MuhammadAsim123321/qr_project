@model Identity_Login.Models.dbModels.RouterJob

@{
    ViewData["Title"] = "Delete Router Job";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg">
                <div class="card-header bg-danger text-white">
                    <h3 class="mb-0">Delete Router Job</h3>
                </div>
                <div class="card-body">
                    <h5 class="mb-3 text-danger">Are you sure you want to delete this router job?</h5>
                    <dl class="row mb-4">
                        <dt class="col-sm-4">Job Number</dt>
                        <dd class="col-sm-8">@Model.JobNumber</dd>
                        <dt class="col-sm-4">Customer Name</dt>
                        <dd class="col-sm-8">@Model.CustomerName</dd>
                        @* <dt class="col-sm-4">QR Code Data</dt> *@
                        @* <dd class="col-sm-8">@Model.QrCodeData</dd> *@
                        @* <dt class="col-sm-4">Job Status</dt> *@
                        @* <dd class="col-sm-8">@Model.Status</dd> *@
                        <dt class="col-sm-4">QR Image</dt>
                        <dd class="col-sm-8">
                            @if (!string.IsNullOrEmpty(Model.PdfFilePath))
                            {
                                var fileName = System.IO.Path.GetFileName(Model.PdfFilePath);
                                var imageUrl = Url.Content($"~/qrs/{fileName}");
                                <img src="@imageUrl" alt="QR" class="img-thumbnail" style="max-width: 120px; max-height: 120px;" />
                            }
                            else
                            {
                                <span class="text-muted">No image generated</span>
                            }
                        </dd>
                    </dl>
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="JobId" />
                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Cancel
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Delete
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>